name: 构建并发布 Docker 镜像

on:
  push:
    tags:
      - 'v*'  # 当推送以 v 开头的 tag 时触发

env:
  # 使用 docker.io 作为 Docker Hub 的注册表
  DOCKER_REGISTRY: docker.io
  # 使用 GitHub Container Registry
  GHCR_REGISTRY: ghcr.io
  # Docker Hub 使用个人账号下的固定镜像仓库
  DOCKER_IMAGE_NAME: dushixiang/pika
  # GHCR 保持使用当前 GitHub 仓库名，以匹配 GITHUB_TOKEN 的包写入权限
  GHCR_IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # 获取版本号
      - name: 获取版本号
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

      # 检出代码
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 读取默认主题锁定版本
        run: echo "DEFAULT_THEME_REF=$(tr -d '\r\n' < .github/default-theme.ref)" >> "$GITHUB_ENV"

      # 默认主题作为独立源码仓库参与 Pika 发布构建
      - name: 检出官方默认主题
        uses: actions/checkout@v4
        with:
          repository: pika-monitor/pika-default-theme
          ref: ${{ env.DEFAULT_THEME_REF }}
          path: pika-default-theme

      # 安装 make 和 upx
      - name: 安装 make 和 upx
        run: |
          sudo apt-get update
          sudo apt-get install -y make upx-ucl

      # 设置 Golang
      - name: 设置 Golang
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'

      # 设置 Node.js
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # 构建所有组件（前端、agents、服务端）
      - name: 构建所有组件
        run: |
          make DEFAULT_THEME_DIR=pika-default-theme build-release

      # 列出编译产物
      - name: 列出编译产物
        run: |
          ls -lh ./bin/
          echo "---"
          ls -lh ./bin/agents/

      # 设置 QEMU 支持多平台构建
      - name: 设置 QEMU
        uses: docker/setup-qemu-action@v3

      # 设置 Docker Buildx
      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 登录 Docker Hub
      - name: 登录 Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # 登录 GitHub Container Registry
      - name: 登录 GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 提取 Docker 元数据
      - name: 提取 Docker 元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}
            ${{ env.GHCR_REGISTRY }}/${{ env.GHCR_IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      # 构建并推送 Docker 镜像
      - name: 构建并推送 Docker 镜像
        id: build-and-push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          file: Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ env.VERSION }}
          cache-from: |
            type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}:cache
            type=local,src=/tmp/.buildx-cache
          cache-to: |
            type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}:cache,mode=max
            type=local,dest=/tmp/.buildx-cache

      # 生成构建总结
      - name: 生成构建总结
        run: |
          echo "## Docker 镜像构建成功 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**版本:** ${{ env.VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**镜像标签:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
